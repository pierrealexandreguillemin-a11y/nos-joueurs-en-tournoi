# Upstash Redis - Base de données Redis pour sync multi-devices
# (Anciennement Vercel KV, maintenant via Upstash Marketplace)
# Ces variables sont automatiquement injectées par Vercel en production
# Pour le développement local, créez un fichier .env.local et remplissez ces valeurs

# Obtenir ces valeurs depuis le dashboard Vercel:
# 1. Allez sur https://vercel.com/dashboard
# 2. Sélectionnez votre projet "hay-chess-tracker"
# 3. Onglet "Storage" > "Connect Store" > Upstash Redis
# 4. Créez une nouvelle base Redis ou connectez-en une existante
# 5. Les variables ci-dessous seront automatiquement ajoutées
# 6. Pour le développement local: Settings > Environment Variables > .env.local

# Variables utilisées par @upstash/redis
KV_REST_API_URL=
KV_REST_API_TOKEN=

# GitHub (pour Claude Code Web)
# Ces valeurs permettent à Claude Code Web de synchroniser avec votre repo
# Obtenir depuis: https://github.com/settings/tokens (Personal access tokens > Tokens classic)
# Permissions requises: repo (Full control of private repositories)

GITHUB_TOKEN=

# Vercel (pour déploiements depuis Claude Code Web)
# Obtenir depuis: https://vercel.com/account/tokens
VERCEL_TOKEN=

# Project Info (pour Claude Code Web)
VERCEL_PROJECT_ID=
VERCEL_ORG_ID=
